import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container:{
        alignItems: 'center',
        backgroundColor: '#FFFFFF',
        marginBottom: 10,
        borderRadius: 20,
        padding: 18,
    },
    image: {
        width: 24,
        height: 24,
    },
});
